---
layout: article_collection
---

<% page_articles.each_with_index do |article, i| %>
  <h3><%= link_to article.title, article %></h3>
  <%= article.summary(250) %>
  <a href="<%= article.url %>">Read more</a>
  <%= tag :hr unless (i == (page_articles.size-1)) %>
<% end %>
